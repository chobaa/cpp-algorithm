#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector<vector<int>> v;
    int t, n, m;
    while (t--)
    {
        cin >> n >> m;
        vector<vector<int>> v(1001, vector<int>(1001, 0));
    }
}